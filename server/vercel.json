{
  "version": 2,
  "builds": [
    {
      "src": "/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/uploads/(.*)", "dest": "/uploads/$1" },
    { "handle": "filesystem" }, 
    { "src": "/(.*)", "dest": "/" } 
  ]
}
